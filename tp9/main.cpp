#include "DynaVector.h"
#include <iostream>
using namespace std;


int main(){

    float a = 0.5, b = 1.0, c = 2.5;
    DynaVector<float> vecteur1 = DynaVector(2);
    vecteur1.ajouterValeur(a);
    vecteur1.ajouterValeur(b);
    cout << "vecteur 1 : " << vecteur1.valeurs{
    if(nb>=taille-1){
        agrandirTab();
    }
    valeurs[nb] = v;
    nb++;
}[0] << endl;

    vecteur1.ajouterValeur(c);


    return 0;
}